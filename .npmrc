; Override Replit's account-level `omit=dev` so that fresh extractions of this
; project always install the dev dependencies needed to run the dev workflows
; (tsx, drizzle-kit, babel-plugin-module-resolver, typescript, etc.).
; `include` overrides `omit`, so this is sufficient.
include=dev
